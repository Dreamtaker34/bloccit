<h1>Edit Question</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for asking questions</p>
    <ul>
      <li>Provide good information on what is happening.</li>
      <li>Make sure to make it clear what your end goal is.</li>
      <li>Don't expect an answer right away. Other people have lives too. :) </li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @question do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Have a question to ask?" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Tell us about it." %>
      </div>
      <div class="form-group">
        <%= f.label :resolved %>
        <%= f.check_box :resolved %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
      <% end %>
    </div>
  </div>
